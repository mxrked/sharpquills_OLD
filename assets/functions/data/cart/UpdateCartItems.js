/**
 *
 *  This is the function that updates the cart items
 *
 */

import {
  USER_CART_ITEMS,
  USER_CART_QUANTITIES,
  USER_CART_SUBTOTALS,
} from "@/assets/data/variables/ARRAYS";

export default function UpdateCartItems() {}
